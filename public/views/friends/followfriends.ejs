<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script
  src="https://code.jquery.com/jquery-3.5.1.min.js"
  integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
  crossorigin="anonymous"></script>
    <title>Follow Friends</title>
</head>
<body>
    <div>
        Welcome <%=user.name%>, please select the people you want to follow:<br>
        <form action="/addfriends" method="POST">
            
            <%let i=0%>
            <%for(i=0;i<database.length;i++){%>
                <%if(database[i].name!=user.name){%>
                    <%let existing = user.following.find(d=>d.friend==database[i]._id)%>
                    <%console.log(existing,database[i]._id)%>
                    <%if(existing===undefined){%>
                    <input type="radio" name="<%=makeid(4)%>" value="<%=database[i]._id%>"><%=database[i].name%><br>
                    <%}}%>
            <%}%>

        <input type="submit">
        </form>   
        <script>
            $(document).on("click", "input[type='radio']", function(){
                thisRadio = $(this);
                if (thisRadio.hasClass("imChecked")) {
                    thisRadio.removeClass("imChecked");
                    thisRadio.prop('checked', false);
                } else { 
                    thisRadio.prop('checked', true);
                    thisRadio.addClass("imChecked");
                };
            })
            </script> 
    </div>
</body>
</html>